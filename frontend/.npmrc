# This is a public open-source project, so it resolves against the public npm registry rather
# than whatever mirror happens to be configured on the machine. A corporate Artifactory mirror
# typically lags npmjs by a patch or two, which makes an otherwise valid lockfile unresolvable
# on someone else's laptop.
registry=https://registry.npmjs.org/
