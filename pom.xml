<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.1</version>
    <relativePath/>
  </parent>

  <groupId>be.kleisli</groupId>
  <artifactId>workspace-watcher</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>workspace-watcher</name>
  <description>Watch what AI agents actually do in your workspace</description>

  <properties>
    <java.version>25</java.version>
    <dgs.version>12.0.1</dgs.version>
    <dgs-codegen.version>3.9.0</dgs-codegen.version>
    <spotless.version>3.10.1</spotless.version>
    <!--
      DGS 12 wires json-path's Jackson3JsonProvider, which first ships in json-path 3.0.0.
      Spring Boot 4.1.1 manages 2.10.0, so without this override the context fails to start with
      NoClassDefFoundError: com/jayway/jsonpath/spi/json/Jackson3JsonProvider.
    -->
    <json-path.version>3.0.0</json-path.version>
    <sqlite-jdbc.version>3.53.4.0</sqlite-jdbc.version>
    <frontend-maven-plugin.version>2.0.2</frontend-maven-plugin.version>
    <maven-clean-plugin.version>3.5.0</maven-clean-plugin.version>
    <node.version>v22.15.0</node.version>
    <!-- mvn package -DskipFrontend to build the backend alone. -->
    <skipFrontend>false</skipFrontend>
    <google-java-format.version>1.36.1</google-java-format.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.netflix.graphql.dgs</groupId>
        <artifactId>graphql-dgs-platform-dependencies</artifactId>
        <version>${dgs.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-graphql</artifactId>
    </dependency>
    <!--
      DGS running on top of Spring for GraphQL, not beside it. The classic
      graphql-dgs-spring-boot-starter brings its own runtime and has been frozen at 9.2.2;
      this one is on 12.x and is what Netflix points new projects at.
    -->
    <dependency>
      <groupId>com.netflix.graphql.dgs</groupId>
      <artifactId>graphql-dgs-spring-graphql-starter</artifactId>
    </dependency>
    <!-- Required for GraphQL subscriptions over the graphql-ws protocol on the servlet stack. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-websocket</artifactId>
    </dependency>
    <!--
      One append-only table with a range query over it. Plain JDBC rather than JPA: there is no
      object graph here, and an ORM would be more machinery than the thing it manages.
    -->
    <!-- For ScriptUtils, which applies db/schema.sql. -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>${sqlite-jdbc.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.graphql</groupId>
      <artifactId>spring-graphql-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>io.projectreactor</groupId>
      <artifactId>reactor-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <!--
        The schema is the contract. Java types are generated from it rather than hand-written,
        so a field cannot drift between what the schema promises and what the backend returns.
        The frontend generates its TypeScript from the same file.
      -->
      <plugin>
        <groupId>io.github.deweyjose</groupId>
        <artifactId>graphqlcodegen-maven-plugin</artifactId>
        <version>${dgs-codegen.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <schemaPaths>
            <param>src/main/resources/graphql/schema.graphqls</param>
          </schemaPaths>
          <packageName>be.kleisli.ww.generated</packageName>
          <generateClient>false</generateClient>
          <generateDataTypes>true</generateDataTypes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <compilerArgs>
            <arg>-Xlint:all,-serial</arg>
          </compilerArgs>
        </configuration>
      </plugin>
      <!--
        The frontend is built into src/main/resources/static and ends up inside the jar, so a
        release needs no node at all. node itself is downloaded per project rather than taken from
        the machine, which keeps a contributor's build identical to CI.
      -->
      <!--
        Vite empties src/main/resources/static on every build, but Maven only ever copies into
        target/classes: a file that disappeared from the source stays there. Asset names carry a
        content hash, so every rebuild left another complete bundle behind and the jar shipped all
        of them - measured at 1294 asset files for the 30 that belong, and 97 MB. Removing the
        copy at the start of the build means the resources step re-copies exactly what exists now.
      -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-clean-plugin</artifactId>
        <version>${maven-clean-plugin.version}</version>
        <executions>
          <execution>
            <id>remove-stale-bundles</id>
            <phase>initialize</phase>
            <goals>
              <goal>clean</goal>
            </goals>
            <configuration>
              <!-- Only the copied static directory; `mvn clean` itself still does the usual. -->
              <excludeDefaultDirectories>true</excludeDefaultDirectories>
              <filesets>
                <fileset>
                  <directory>${project.build.outputDirectory}/static</directory>
                </fileset>
              </filesets>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>${frontend-maven-plugin.version}</version>
        <configuration>
          <workingDirectory>frontend</workingDirectory>
          <skip>${skipFrontend}</skip>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
            </configuration>
          </execution>
          <execution>
            <id>npm-ci</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>ci --no-audit --no-fund</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-build</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <!--
        Google Java Style, applied by a tool rather than by hand. `mvn spotless:apply` formats,
        and the build fails on anything unformatted so style never becomes review chatter.
        google-java-format reaches into javac internals, hence the flags in .mvn/jvm.config.
      -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <includes>
              <include>src/main/java/**/*.java</include>
              <include>src/test/java/**/*.java</include>
            </includes>
            <googleJavaFormat>
              <version>${google-java-format.version}</version>
              <style>GOOGLE</style>
              <reflowLongStrings>true</reflowLongStrings>
            </googleJavaFormat>
          </java>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
